name: sucess job2
on:
  workflow_call:
jobs:
  fail:
    environment:
      name: e2e
    runs-on: ubuntu-latest
    steps:
      - name: force pass
        run: echo "bom dia"
      - name: check the run status
        if: success()
        run: echo "this job has sucess"